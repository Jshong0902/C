// 4.cpp : 이 파일에는 'main' 함수가 포함됩니다. 거기서 프로그램 실행이 시작되고 종료됩니다.
//

#include <stdio.h>
#define _CRT_SECURE_NO_WARNINGS

int main()
{
    int a, b;
    scanf("%d %d", &a, &b);
    if (a < b) {
        printf("%d < %d", a, b);
    }
    else if (a > b) {
        printf("%d > %d", a, b);
    }
    else if {
        printf("%d = %d", a, b);
    }
}
